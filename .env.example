# Browser and server runtime (safe, public project credentials)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Seed script only. Keep this server-side and never expose it to the browser.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Added in Phase 4. The app must continue to work when this is absent.
GROQ_API_KEY=
